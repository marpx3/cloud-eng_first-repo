### LEARNINGs

root main <> modul main
-> root main liefert die variablen, alle variablen müssen im module ordner definiert sein um sie übergeben zu können -> module main führt aus

-> output muss auch in root erstellt werden dann kann value über das output module gecalled werden

Freigabe möglichkeit kann üer github envs implementiert werden dann müssen aber die FedCred angepasst werden

Henne-Ei Problem neue Bootstrap schicht muss lokal deployed werden (tfstate storage, FedCred)

Race Connditions können über concurrency gruppen gehandelt werden

Auto Comments via hithub scripts: so das der Github Bot die Terraform Changes auch als kommentar an den PR hängt
