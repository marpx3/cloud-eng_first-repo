#cloud-config
package_update: true
packages:
  - nginx

write_files:
  - path: /var/www/html/index.html
    content: |
      <html>
        <body>
          <h1>Deployed by deploy.sh</h1>
          <p>Diese VM hat sich per cloud-init selbst konfiguriert.</p>
        </body>
      </html>
